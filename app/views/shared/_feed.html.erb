<% if @feed_items.any? %>
  <ul class="media-list">
  	<!--  The status feed partial defers the rendering to the micropost partial. 
  	Rails knows to call the micropost partial because each element of 
  	@feed_items has class Micropost. This causes Rails to look for a partial 
  	with the corresponding name in app/views/microposts/_micropost.html.erb -->
    <%= render @feed_items %>
  </ul>
  <%= will_paginate @feed_items %>
<% end %>